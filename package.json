{
  "name": "kurongeka",
  "version": "2.0.0",
  "private": true,
  "description": "kurongeka.com: launch-and-run service for small businesses, by Kapture. Astro frontend on a Wix Headless backend.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "verify": "npm run check && npm run test && npm run build"
  },
  "dependencies": {
    "@astrojs/vercel": "^11.0.11",
    "@fontsource-variable/wix-madefor-display": "^5.3.0",
    "@fontsource-variable/wix-madefor-text": "^5.3.0",
    "@wix/data": "^1.0.522",
    "@wix/forms": "^1.0.522",
    "@wix/pricing-plans": "^1.0.403",
    "@wix/sdk": "^1.21.16",
    "astro": "^7.3.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "1.56.1",
    "@types/node": "^22.20.4",
    "typescript": "^6.0.3",
    "vitest": "^5.0.2"
  }
}
